# Firebase

Firebase пока не подключён к реальному проекту. Правила Firestore и Storage по умолчанию запрещают любой доступ.

Файлы `google-services.json` не должны попадать в Git. Отдельные конфигурации `dev` и `prod` будут добавлены после создания двух Firebase-проектов.

